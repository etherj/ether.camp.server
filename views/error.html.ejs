<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Ethereum Studio</title>
    <link rel="stylesheet" type="text/css" href="/static/plugins/ethergit.libs/bootstrap/css/bootstrap.css">
    <link rel="stylesheet" type="text/css" href="/static/plugins/ethergit.libs/bootstrap/css/bootstrap-theme.css">

    <style>
      body {
      background-color: rgb(34, 34, 34);
      color: white;
      padding-top: 30px;
      }
      a {
      color: #B9CA4A;
      }
    </style>
  </head>
  <body>
    <div class="container bs-root">
      <div class="row">
        <h2>Sorry, we are having temporary server issues</h2>
      </div>
      <div class="row">
        <p><%= message %></p>
        <% if (temp) { %>
        <p>The page will be reloaded in <span id="secs">15</span> seconds. Or you can <a href="/">reload it manually</a>.</p>
        <% } %>
      </div>
      <div class="row">
        <p>Ask us anything in <a target="_blank" href="http://www.ether.camp">our slack</a>.</p>
      </div>
    </div>

    <% if (temp) { %>
    <script>
      var secs = 15;
      setInterval(function() {
        var counter = document.getElementById('secs').textContent = --secs;
        if (secs == 0) {
          clearInterval(counter);
          location.reload();
        }
      }, 1000);
    </script>
    <% } %>
  </body>
</html>
